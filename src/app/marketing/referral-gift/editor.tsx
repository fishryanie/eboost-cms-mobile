import { CmsEditorRoute } from 'shared/cms-pages/cms-editor-route';

export default function ReferralGiftEditorPage() {
  return <CmsEditorRoute fallbackHref='/marketing/referral-gift' pageKey='referral-gift' />;
}
